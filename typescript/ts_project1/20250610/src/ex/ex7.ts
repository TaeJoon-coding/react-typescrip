// デフォルト定義方法
// タイプごとの別称を定義できる。
// class定義のようなもの。

type MyString = string;
type MyNumber = number;
type MyUnion = string | null;
type MyObj = { a: 1 } | { b: 2 };
type MyTuple = [string, boolean];
